# features/

User-facing interactions with real business meaning (e.g. `add-to-cart`,
`place-bid`, `checkout`). Each feature slice typically has `ui/`, `model/`,
and `api/` segments. Empty until the first feature is implemented.
