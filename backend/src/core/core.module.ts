import { Module } from '@nestjs/common';

// Home for future cross-cutting providers (correlation IDs, logging, global filters/guards) — empty until one is actually implemented.
@Module({})
export class CoreModule {}
