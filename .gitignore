# Dependencies
node_modules/

# Build output
dist/
dist-ssr/
build/
coverage/

# Env files — never commit real secrets, only .env.example templates
.env
.env.*
!.env.example

# Logs
logs
*.log

# Uploaded product images (local disk storage — see
# backend/src/modules/products/infrastructure/product-image.multer-config.ts)
backend/uploads/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# TypeScript build info
*.tsbuildinfo

# Local Claude Code settings (machine-specific permissions, not shared config)
.claude/settings.local.json

# Editor / OS
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

*storybook.log
storybook-static
